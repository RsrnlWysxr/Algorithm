选择排序： O(n^2)，
          稳定
          循环次数已知
          
插入排序： O(n^2)，
          原地排序
          稳定
          优化：每次循环改为后移元素，一次赋值操作代替了交换
          在近乎有序情况下，性能良好
          
归并排序： O(nlogn)
          空间O(n+logn)--> 新开n个空间，logn为递归调用
          稳定
          优化：每次合并前对两部分进行对比
          在近乎有序情况下，性能良好
          
归并排序（自底向上）：
          迭代实现，思路可用于链表

快速排序： O(nlogn)
          空间O(logn)--> logn为递归调用
          不稳定
          优化： 1.递归至数组内元素少于16时，进行插入排序
                2.避免退化为链表，起始标定点随机选择
                3.优化了多重复元素数组，两路快排与三路快排
          
 堆排序： O(nlogn)
         原地
         不稳定
         优化：思路同插入排序，每次shiftdown操作中，每次循环不交换，只赋值，出循环最后赋值
